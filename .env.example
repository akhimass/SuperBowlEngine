# Copy to `.env` and fill in locally. Do not commit `.env`.

# CollegeFootballData API — college player season stats for the draft pipeline
# https://collegefootballdata.com/key
CFBD_API_KEY=

# Optional alias
# GRIDIRONIQ_CFBD_API_KEY=

# Folder containing real draft board CSV/JSON files (consensus + variance); never commit proprietary boards
# GRIDIRONIQ_DRAFT_CONSENSUS_DIR=

# FastAPI CORS — extra allowed origins (comma-separated), e.g. your Vercel production URL
# GRIDIRONIQ_CORS_ORIGINS=https://gridironiq.vercel.app

# Optional: override regex for browser Origin (default allows https://*.vercel.app). Set empty in shell to disable regex:
# GRIDIRONIQ_CORS_ORIGIN_REGEX=
