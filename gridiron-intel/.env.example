# Production / preview: your deployed GridironIQ FastAPI base URL (no trailing slash).
# Example: https://gridironiq-api.onrender.com
# Leave unset for local dev (falls back to http://127.0.0.1:8000 in api.ts).
VITE_API_BASE_URL=
